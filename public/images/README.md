# Image assets

Drop site images here. They are served from `/images/<name>` (the `public/`
folder is the web root).

## Required

- `hero-graduate.png` — landing hero. The graduate-between-Africa-and-Europe
  photo. Used full-bleed by `src/app/page.tsx`. Recommended: ~2000px wide,
  optimized PNG/WebP.
